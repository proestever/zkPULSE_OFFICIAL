# Relayer Configuration
# Copy this file to .env and fill in your values

# Required: Your relayer's private key (keep this secret!)
RELAYER_PRIVATE_KEY=your_private_key_here

# Required: Your relayer's address (derived from private key)
RELAYER_ADDRESS=your_relayer_address_here

# Optional: RPC URL for PulseChain (defaults to public WebSocket)
RPC_URL=wss://ws.pulsechain.com

# Optional: Port for relayer server (defaults to 4000)
RELAYER_PORT=4000

# Note: Make sure your relayer address has enough PLS to pay for gas fees!