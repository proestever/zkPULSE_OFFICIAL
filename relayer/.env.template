# zkPulse Relayer Configuration Template
# Copy this file to .env and fill in your values

# REQUIRED - Your relayer's private key (keep this SECRET!)
RELAYER_PRIVATE_KEY=your_private_key_here

# REQUIRED - Your relayer's address
RELAYER_ADDRESS=your_relayer_address_here

# Optional - RPC endpoint (default: https://rpc.pulsechain.com)
RPC_URL=https://rpc.pulsechain.com

# Optional - Port to run relayer on (default: 4000)
RELAYER_PORT=4000

# Optional - Allowed origins for CORS (comma-separated)
# Example: https://yourdomain.com,https://app.yourdomain.com
ALLOWED_ORIGINS=http://localhost:8888,http://localhost:3000

# Optional - Node environment (production/development)
NODE_ENV=production

# SECURITY NOTES:
# 1. NEVER commit your actual .env file to git
# 2. Add .env to your .gitignore file
# 3. Use a different private key for production
# 4. Keep your private key secure and never share it
# 5. Update ALLOWED_ORIGINS for production domains only